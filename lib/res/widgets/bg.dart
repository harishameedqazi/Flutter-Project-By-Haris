import 'package:flutter/material.dart';

class BgComponent extends StatelessWidget {
  const BgComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}